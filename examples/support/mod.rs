//! Shared, non-library data used by multiple `examples/*.rs` binaries so the
//! same sample dataset can be rendered with different table styles without
//! duplicating it. Not part of the public crate API.

pub mod gh_runs;
